#include <iostream>

/*
 * Programa para ver suma binaria
*/

int main() {
  char a = +23;
  char b = -23;
  char c = a + b;
  std::cout << "Operando a" << +a << std::endl;
  std::cout << "Operando b" << +b << std::endl;
  std::cout << "Suma binaria en C " << +c << std::endl;
  return 0;
}